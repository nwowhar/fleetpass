{
  "name": "@fleetpass/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Canonical domain types + the checklist-template cascade resolver, shared across the worker app, dashboard, console, and Cloud Functions.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
